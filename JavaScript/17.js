// WHILE LOOP
// the same as Java 

// some different
age = 10
do{
    console.log(`You are ${age} years old this years`);
    age++;
}while(age <= 18);


let arr = ["A", "B", "C"];

while (arr.length){    // run until arr.length == 0  ==> false
    console.log(arr.pop());      // then stop
}